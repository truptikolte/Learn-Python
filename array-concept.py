# array is same as list but only collection of same data type
# array is memory efficient but slower than lists
# declare the array(always mention the data type)
import array as arr
a=arr.array('i',[2,4,6,8,9])
print(a)

from array import *
b= array('I',[5,6,7])
print(b)

c=array('f',[2.01,2.44,5.33])
print(c)

#char=array('B',['a','b','c'])